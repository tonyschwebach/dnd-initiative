const Character = require('/Character');

const characters =[];

const bj = new Character('BJ',20,4);
const yamis = new Character('Yamis',14,2);

// function to create new Character with form inputs
// const addCharacter = (name,initiative,dexMod) =>{
  const addCharacter = (character) =>{ //temp for placeholder data
  characters.push(character)
}

// function to render characters
// loop over charactersArray
// sort by getInitiative, then getDexMod

// function to show turns
// show first character as current turn
// show next character as next


// event listener for add character to show form

// event listener for submit character

// event listener for next turn


// edge case: character added mid battle
// tracker for current turn?
// add/insert new character to charactersArray and in display order. 
// keep same turn 

